<?xml version="1.0" encoding="UTF-8"?>
<compiled-workflow>
<mission><![CDATA[

Initialize test framework with Playwright or Cypress. Use when the user says "lets setup test framework" or "I want to initialize testing framework"

Mode: Create
Target: Story 10.1

]]></mission>
<tea-paths>
<!-- bmad-assist pre-resolved these from _bmad/tea/config.yaml so step files referencing {test_artifacts} (and FUTURE siblings) have concrete values without needing runtime token resolution. -->
<test_artifacts>/private/var/folders/c8/4hyhkyxn3xz219khx9yp4l100000gp/T/bmad-assist-snapshot-projects/bmad-testarch-framework/proj/_bmad-output/test-artifacts</test_artifacts>
<test_design_output>_bmad-output/test-artifacts/test-design</test_design_output>
<test_review_output>_bmad-output/test-artifacts/test-reviews</test_review_output>
<trace_output>_bmad-output/test-artifacts/traceability</trace_output>
</tea-paths>
<context>
<file id="f7436b0d" path="docs/project_context.md" label="PROJECT CONTEXT"><![CDATA[

# Project Context

Minimal context.


]]></file>
<file id="2971f9e6" path="docs/architecture.md" label="ARCHITECTURE"><![CDATA[

# Architecture

Layered.


]]></file>
<file id="a968a91e" path="docs/sprint-artifacts/10-1-initial-setup.md" label="STORY FILE"><![CDATA[

# Story 10.1: Initial Setup

## Status

ready-for-dev

## Acceptance Criteria

- [ ] AC1

## Tasks/Subtasks

- [ ] Task 1

## Dev Notes

Minimal notes.


]]></file>
</context>
<variables>
<var name="epic_num">10</var>
<var name="knowledgeIndex">/Users/derron.carr/Documents/Folder/bmad-assist/src/bmad_assist/testarch/knowledge_base/tea-index.csv</var>
<var name="step_01_preflight_nextStepFile">.claude/skills/bmad-testarch-framework/steps-c/{skill-root}/steps-c/step-02-select-framework.md</var>
<var name="story_file" file_id="a968a91e">embedded in prompt, file id: a968a91e</var>
<var name="story_id">10.1</var>
<var name="story_num">1</var>
<var name="tea_use_mcp_enhancements">True</var>
<var name="tea_use_playwright_utils">True</var>
<var name="workflow_mode">c</var>
</variables>
<instructions><![CDATA[

<!-- STEP: step-01-preflight -->
# Step 1: Preflight Checks

## STEP GOAL

Verify the project is ready for framework scaffolding and gather key context.

## MANDATORY EXECUTION RULES

- 📖 Read the entire step file before acting
- ✅ Speak in `{communication_language}`
- 🚫 Halt if preflight requirements fail

---

## EXECUTION PROTOCOLS:

- 🎯 Follow the MANDATORY SEQUENCE exactly
- 💾 Record outputs before proceeding
- 📖 Load the next step only when instructed

## CONTEXT BOUNDARIES:

- Available context: config, loaded artifacts, and knowledge fragments
- Focus: this step's goal only
- Limits: do not execute future steps
- Dependencies: prior steps' outputs (if any)

## MANDATORY SEQUENCE

**CRITICAL:** Follow this sequence exactly. Do not skip, reorder, or improvise.

## 1. Stack Detection

**Read `config.test_stack_type`** from `{config_source}`.

**Auto-Detection Algorithm** (when `test_stack_type` is `"auto"` or not configured):

- Scan `{project-root}` for project manifests:
  - **Frontend indicators**: `package.json` with react/vue/angular/next dependencies, `playwright.config.*`, `vite.config.*`, `webpack.config.*`
  - **Backend indicators**: `pyproject.toml`, `pom.xml`/`build.gradle`, `go.mod`, `*.csproj`/`*.sln`, `Gemfile`, `Cargo.toml`
  - **Both present** = `fullstack`; only frontend = `frontend`; only backend = `backend`
- Explicit `test_stack_type` config value overrides auto-detection
- **Backward compatibility**: if `test_stack_type` is not in config, treat as `"auto"` (preserves current frontend behavior for existing installs)

Store result as `{detected_stack}` = `frontend` | `backend` | `fullstack`

---

## 2. Validate Prerequisites

**If {detected_stack} is `frontend` or `fullstack`:**

- `package.json` exists in project root
- No existing E2E framework (`playwright.config.*`, `cypress.config.*`, `cypress.json`)

**If {detected_stack} is `backend` or `fullstack`:**

- At least one backend project manifest exists (`pyproject.toml`, `pom.xml`, `build.gradle`, `go.mod`, `*.csproj`, `Gemfile`, `Cargo.toml`)
- No existing test framework config that conflicts (e.g., `conftest.py` with full pytest suite, `src/test/` with JUnit suite)

- Architecture/stack context available (project type, bundler, dependencies)

If any fail, **HALT** and report the missing requirement.

---

## 3. Gather Project Context

**If {detected_stack} is `frontend` or `fullstack`:**

- Read `package.json` to identify framework, bundler, dependencies

**If {detected_stack} is `backend` or `fullstack`:**

- Read the relevant project manifest (`pyproject.toml`, `pom.xml`, `go.mod`, `*.csproj`, `Gemfile`, `Cargo.toml`) to identify language, framework, and dependencies

- Check for architecture docs (`architecture.md`, `tech-spec*.md`) if available
- Note auth requirements and APIs (if documented)

---

## 3. Confirm Findings

Summarize:

- Project type and bundler
- Whether a framework is already installed
- Any relevant context docs found

---

### 4. Save Progress

**Save this step's accumulated work to `{outputFile}`.**

- **If `{outputFile}` does not exist** (first save), create it with YAML frontmatter:

  ```yaml
  ---
  stepsCompleted: ['step-01-preflight']
  lastStep: 'step-01-preflight'
  lastSaved: '{date}'
  ---
  ```

  Then write this step's output below the frontmatter.

- **If `{outputFile}` already exists**, update:
  - Add `'step-01-preflight'` to `stepsCompleted` array (only if not already present)
  - Set `lastStep: 'step-01-preflight'`
  - Set `lastSaved: '{date}'`
  - Append this step's output to the appropriate section of the document.

Load next step: `{nextStepFile}`

## 🚨 SYSTEM SUCCESS/FAILURE METRICS:

### ✅ SUCCESS:

- Step completed in full with required outputs

### ❌ SYSTEM FAILURE:

- Skipped sequence steps or missing outputs
  **Master Rule:** Skipping steps is FORBIDDEN.


]]></instructions>
<output-template></output-template>
</compiled-workflow>